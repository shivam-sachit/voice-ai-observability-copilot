// Scores one transcript against an agent's active KPIs. Every verdict and use-action must
// cite an exact transcript quote + turn index (so the UI can highlight the segment).
// --- Implemented in Task 7. ---
// Contract: analyzeTranscript(transcript, activeKpis) -> AnalysisResult (matches ANALYSIS_SCHEMA)
export {}
