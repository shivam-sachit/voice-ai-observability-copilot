// Reads an agent's script/goals and asks Claude to propose KPIs (each with a testable rubric).
// These come back as status:'proposed'; the user confirms them in the KPI Config UI.
// --- Implemented in Task 7. ---
// Contract: suggestKpis(agent) -> Kpi[]   (matches KPI_SUGGESTION_SCHEMA)
export {}
